@using ljgb.Common.Responses;
@{
    ViewData["Title"] = "Daftar Transaksi";
    ViewData["PageName"] = "provinsi_provinsi";
    ViewData["Category1"] = "Provinsi";
    var data = (TransactionResponse)ViewData["Response"];
    int nomor = 0;

}

@section Header {
}

@section PageHeading {
    <div class="subheader">
        <h1 class="subheader-title">
            <i class="fal fa-th-list text-primary"></i> Provinsi
            @*<small>
                    Documentation and examples for opt-in styling of tables (given their prevalent use in JavaScript plugins) with Bootstrap.
                </small>*@
        </h1>
    </div>
}
<style>
    .modal-dialog {
        height: 80% !important;
        padding-top: 10%;
    }

    .modal-content {
        height: 100% !important;
        overflow: visible;
    }

    .modal-body {
        height: 80%;
        overflow: auto;
    }
</style>

<div class="row">
    <div class="col-lg-12 col-xl-12">


        <!--Table head-->
        <div id="panel-2" class="panel">
            <div class="panel-hdr">
                <h2>
                    Table <span class="fw-300"><i>head</i></span>
                </h2>
                <div class="panel-toolbar">
                    <button class="btn btn-panel" data-action="panel-collapse" data-toggle="tooltip" data-offset="0,10" data-original-title="Collapse"></button>
                    <button class="btn btn-panel" data-action="panel-fullscreen" data-toggle="tooltip" data-offset="0,10" data-original-title="Fullscreen"></button>
                    <button class="btn btn-panel" data-action="panel-close" data-toggle="tooltip" data-offset="0,10" data-original-title="Close"></button>
                </div>
            </div>
            <div class="panel-container show">
                <div class="panel-content">
                    <div class="panel-tag">
                        Similar to default and inverse tables, use one of two modifier classes to make <code>&lt;thead&gt;</code>s appear custom, dark gray, white and themed.
                    </div>
                    <h5 class="frame-heading">
                        
                    </h5>
                    <div class="frame-wrap">
                        <table class="table m-0">
                            <thead class="thead-dark">
                                <tr>
                                    <th>Status</th>
                                    <th>Nama Pembeli</th>
                                    <th>Nama Penjual</th>
                                    <th>Nama Barang</th>
                                    <th>Harga Nego</th>
                                    <th>Harga OTR</th>
                                    <th>Action</th>



                                </tr>
                            </thead>
                            <tbody>
                                @if (data != null)
                                {
                                    @foreach (var item in data.ListTransaction)
                                    {
                                <tr>

                                    <td>@item.TrasanctionLevel.Status.Name</td>
                                    <td>@item.Buyer.Name</td>
                                    <td>@item.Seller.Name</td>
                                    <td>@item.NegoBarang.Barang.Name</td>
                                    <td>@item.NegoBarang.Harga</td>
                                    <td>@item.NegoBarang.Barang.HargaOtr</td>
                                    <th scope="row" style="text-align:right">
                                        <button type="button" class="btn btn-secondary waves-effect waves-themed fal fa-edit" id="@item.ID" onclick="Cancel(this.id)">Cancel </button>
                                        <button type="button" class="btn btn-primary waves-effect waves-themed fal fa-edit" id="@item.ID" onclick="Approve(this.id)" > @item.TrasanctionLevel.Step.Name </button>

                                       

                                    </th>

                                </tr>

                                    }
                                }


                            </tbody>
                        </table>
                    </div>

                </div>
            </div>
        </div>

    </div>
    @*<div class="row">
            <div class="col-xl-12">
                <div id="panel-1" class="panel">
                    <div class="panel-hdr">
                        <h2>
                            Example <span class="fw-300"><i>Table</i></span>
                        </h2>
                        <div class="panel-toolbar">
                            <button class="btn btn-panel" data-action="panel-collapse" data-toggle="tooltip" data-offset="0,10" data-original-title="Collapse"></button>
                            <button class="btn btn-panel" data-action="panel-fullscreen" data-toggle="tooltip" data-offset="0,10" data-original-title="Fullscreen"></button>
                            <button class="btn btn-panel" data-action="panel-close" data-toggle="tooltip" data-offset="0,10" data-original-title="Close"></button>
                        </div>
                    </div>
                    <div class="panel-container show">
                        <div class="panel-content">
                            <!-- datatable start -->
                            <table id="dt-basic-example" class="table table-bordered table-hover table-striped w-100">
                                <thead>
                                    <tr>
                                        <th>Name</th>
                                        <th>Description</th>
                                        <th>Action</th>
                                    </tr>
                                </thead>
                                <tbody></tbody>
                            </table>
                            <!-- datatable end -->
                        </div>
                    </div>
                </div>
            </div>
        </div>*@
</div>

<div class="modal fade" id="myModal" role="dialog">
    <form>
        <div class="modal-dialog">

            <!-- Modal content-->
            <div class="modal-content">
                <div class="modal-header">

                    <h4 class="modal-title">Maintain Provinsi</h4>
                    <button type="button" class="close" data-dismiss="modal">&times;</button>

                </div>
                <div class="modal-body">




                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-warning waves-effect waves-themed" data-dismiss="modal">Cancel</button>
                    <button type="button" id="SaveEdit" class="btn btn-success waves-effect waves-themed"> <span class="fal fa-check mr-1"></span>Submit</button>

                </div>
            </div>

        </div>
    </form>
</div>


<div class="modal fade" id="ModalAdd" role="dialog">
    <form>
        <div class="modal-dialog">

            <!-- Modal content-->
            <div class="modal-content">
                <div class="modal-header">

                    <h4 class="modal-title">Maintain Provinsi</h4>
                    <button type="button" class="close" data-dismiss="modal">&times;</button>

                </div>
                <div class="modal-body">

                    <div class="form-group">
                        <label class="form-label" for="simpleinput">Nama Provinsi</label>

                        <input type="text" id="Nama" name="Nama" class="form-control">
                    </div>
                    <div class="form-group">
                        <label class="form-label" for="example-textarea">Deskripsi</label>
                        <textarea class="form-control" id="DeskripsiProvinsi" name="Description" rows="5"></textarea>
                    </div>

                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-warning waves-effect waves-themed" data-dismiss="modal">Cancel</button>
                    <button type="button" id="btn-save" class="btn btn-success waves-effect waves-themed"> <span class="fal fa-check mr-1"></span>Submit</button>

                </div>
            </div>

        </div>
    </form>
</div>



@section Scripts {
    <script type="text/javascript">
        function Details(id) {
            $.get("@Url.Action("Details", "Provinsi")/" + id,
            function (item) {
                    $('.modal-body').html(item);
                })

            $('#myModal').modal('show');

        }
        function DeleteModel(id) {


            // not sure if you wanted this, but I thought I'd add it.
            // get an associative array of just the values.

            // not sure if you wanted this, but I thought I'd add it.
            // get an associative array of just the values.


            var values = {
                id: id
            };
            $.ajax({
                url: "https://localhost:44325/api/Provinsi/DeletePost",
                contentType: 'application/json',
                dataType: 'json',
                type: "POST",
                data: JSON.stringify(values),
                success: function (data) {
                    console.log(data);
                },
                error: function (jqXHR, text, errorThrown) {
                    console.log(jqXHR + " " + text + " " + errorThrown);
                }
            });


        };
        function Approve(id) {


                // not sure if you wanted this, but I thought I'd add it.
                // get an associative array of just the values.

                // not sure if you wanted this, but I thought I'd add it.
                // get an associative array of just the values.


                var values = {
                    id: id
                };
                $.ajax({
                    url: "https://localhost:44325/api/Transaction/ApproveTransaction",
                    contentType: 'application/json',
                    dataType: 'json',
                    type: "POST",
                    data: JSON.stringify(values),
                    success: function (data) {
                        console.log(data);
                    },
                    error: function (jqXHR, text, errorThrown) {
                        console.log(jqXHR + " " + text + " " + errorThrown);
                    }
                });


            };
        function Cancel(id) {


            // not sure if you wanted this, but I thought I'd add it.
            // get an associative array of just the values.

            // not sure if you wanted this, but I thought I'd add it.
            // get an associative array of just the values.


            var values = {
                id: id
            };
            $.ajax({
                url: "https://localhost:44325/api/Transaction/CancelTransaction",
                contentType: 'application/json',
                dataType: 'json',
                type: "POST",
                data: JSON.stringify(values),
                success: function (data) {
                    console.log(data);
                },
                error: function (jqXHR, text, errorThrown) {
                    console.log(jqXHR + " " + text + " " + errorThrown);
                }
            });


        };
        $(document).ready(function () {
            // initialize datatable
            var url = "@ViewBag.url_api";
            $('#btn-save').on('click', function () {


                // not sure if you wanted this, but I thought I'd add it.
                // get an associative array of just the values.

                // not sure if you wanted this, but I thought I'd add it.
                // get an associative array of just the values.

                var values = {
                    Nama: $('#Nama').val(),
                    Description: $('#DeskripsiProvinsi').val(),
                };

                $.ajax({
                    url: "https://localhost:44325/api/Provinsi/AddPost",
                    contentType: 'application/json',
                    dataType: 'json',
                    type: "POST",
                    data: JSON.stringify(values),
                    success: function (data) {
                        console.log(data);
                    },
                    error: function (jqXHR, text, errorThrown) {
                        console.log(jqXHR + " " + text + " " + errorThrown);
                    }
                });


            });
            $('#SaveEdit').on('click', function () {


                // not sure if you wanted this, but I thought I'd add it.
                // get an associative array of just the values.

                // not sure if you wanted this, but I thought I'd add it.
                // get an associative array of just the values.

                var values = {
                    Nama: $('#Nama').val(),
                    Description: $('#DeskripsiProvinsi').val(),
                    ID: $('#ModelID').val(),
                };

                $.ajax({
                    url: "https://localhost:44325/api/Provinsi/UpdatePost",
                    contentType: 'application/json',
                    dataType: 'json',
                    type: "POST",
                    data: JSON.stringify(values),
                    success: function (data) {
                        console.log(data);
                    },
                    error: function (jqXHR, text, errorThrown) {
                        console.log(jqXHR + " " + text + " " + errorThrown);
                    }
                });


            });
            



        });


    </script>
}